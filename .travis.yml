language: node_js

node_js:
  - '8.9.4'

install:
  - npm install
  - npm run build

deploy:
  on:
    branch: master
  provider: s3
  access_key_id: $AWS_ACCESS_ID
  secret_access_key: $AWS_ACCESS_KEY
  bucket: $BUCKET
  skip_cleanup: true
  local_dir: build
  acl: public_read
